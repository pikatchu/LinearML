

module T: sig

  type 'a t = { field1: int32 * 'a ; field2: float }

  val f: unit -> int32

end = struct

  let f () = 
    let t = { field2 = 1.0 ; field1 = 2, 3 ; } in
    let _, y = t.field1 in
    let { t ; field1 = _, _ } = t in
    free t ;
    y+y
end
