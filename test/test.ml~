
module T: sig

    type t = 
    | Empty
    | B


  val rev_append: t obs * t obs -> t
end = struct

  let rec rev_append l1 l2 = 
    match l1, l2 with
    | Empty, l -> Empty
    | l, Empty -> Empty
    | _, _ -> B



end
