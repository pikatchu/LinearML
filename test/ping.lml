
module Test: sig

end = struct

  let ping cin cout = 
    ignore (Fifo.read cin) ;
    Fifo.write cout "Ping"

  let pong cin cout = 
    ignore (Fifo.read cin) ;
    Fifo.write cout "Pong"

  let make() = 
    let fifo1 = Fifo.make() in
    let fifo2 = Fifo.make() in
    
    
end
